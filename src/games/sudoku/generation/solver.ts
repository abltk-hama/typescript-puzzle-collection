export interface SolveResult{solution:number[]|null;count:number;nodes:number}
export function solveSudoku(input:number[],limit=1,random:()=>number=Math.random):SolveResult{const grid=[...input];let count=0,first:number[]|null=null,nodes=0;function visit(){if(count>=limit)return;let best=-1,bestCandidates:number[]=[];for(let i=0;i<81;i++){if(grid[i])continue;const used=new Set<number>();const row=Math.floor(i/9),col=i%9;for(let n=0;n<9;n++){used.add(grid[row*9+n]);used.add(grid[n*9+col])}const br=Math.floor(row/3)*3,bc=Math.floor(col/3)*3;for(let r=br;r<br+3;r++)for(let c=bc;c<bc+3;c++)used.add(grid[r*9+c]);const options=Array.from({length:9},(_,n)=>n+1).filter(n=>!used.has(n));if(!options.length)return;if(best<0||options.length<bestCandidates.length){best=i;bestCandidates=options;if(options.length===1)break}}if(best<0){count++;first??=[...grid];return}nodes++;bestCandidates.sort(()=>random()-.5);for(const value of bestCandidates){grid[best]=value;visit();grid[best]=0;if(count>=limit)return}}visit();return{solution:first,count,nodes}}
export const countSolutions=(grid:number[],limit=2)=>solveSudoku(grid,limit,()=>.5).count
