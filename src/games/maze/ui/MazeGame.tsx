import { useEffect,useRef,useState } from 'react'
import { GameFooter,MessagePanel } from '../../../common/components/GameChrome'
import { DOWN,LEFT,RIGHT,UP,currentPosition,initialGame,isComplete,keyOf,moveDirection,moveTo,nextHint,samePosition,undo,type MazePuzzle,type MazeState } from '../domain/maze'
import { deleteMazeSession,loadMazeSession,saveMazeSession } from '../data/sessions'
const directionNames:Record<string,string>={'-1,0':'上','1,0':'下','0,-1':'左','0,1':'右'}
export function MazeGame({puzzle,onBack,onLauncher}:{puzzle:MazePuzzle;onBack:()=>void;onLauncher:()=>void}){const[state,setState]=useState<MazeState>(()=>initialGame(puzzle)),[message,setMessage]=useState('ゲームを開始しました。'),[phase,setPhase]=useState<'loading'|'ask'|'play'>('loading'),saved=useRef<MazeState|undefined>(undefined),board=useRef<HTMLDivElement>(null),complete=isComplete(puzzle,state),current=currentPosition(state);useEffect(()=>{loadMazeSession(puzzle).then(session=>{saved.current=session;setPhase(session?'ask':'play')}).catch(()=>{setMessage('保存データを読み込めないため、最初から開始します。');setPhase('play')})},[puzzle]);useEffect(()=>{if(phase!=='play')return;if(complete||(state.history.length===1&&!state.hintCount))deleteMazeSession(puzzle.id);else saveMazeSession(puzzle,state)},[state,complete,puzzle,phase]);useEffect(()=>{if(phase==='play')board.current?.focus()},[phase]);function apply(next:MazeState){if(next===state)return;setState(next);setMessage(isComplete(puzzle,next)?`ゴールに到着しました！（${next.history.length-1}手、ヒント使用 ${next.hintCount}回）`:'一マス進みました。')}function keyDown(event:React.KeyboardEvent){const mapping:Record<string,[number,number]>={ArrowUp:[-1,0],w:[-1,0],W:[-1,0],ArrowDown:[1,0],s:[1,0],S:[1,0],ArrowLeft:[0,-1],a:[0,-1],A:[0,-1],ArrowRight:[0,1],d:[0,1],D:[0,1]};if(mapping[event.key]){event.preventDefault();apply(moveDirection(puzzle,state,...mapping[event.key]))}}if(phase==='loading')return <main className="shell"><section className="panel"><p>保存状態を確認しています…</p></section></main>;if(phase==='ask')return <main className="shell"><section className="panel resume-dialog"><h1>保存済みの進捗</h1><p>この迷路には保存済みの進捗があります。</p><div className="actions"><button className="button" onClick={()=>{setState(saved.current!);setMessage('保存済みの進捗から再開しました。');setPhase('play')}}>続きから</button><button className="button secondary" onClick={()=>{deleteMazeSession(puzzle.id);setState(initialGame(puzzle));setPhase('play')}}>最初から</button><button className="button secondary" onClick={onBack}>キャンセル</button></div></section></main>;const visits=new Set(state.history.map(keyOf));return <main className="shell"><section className="panel"><div className="toolbar"><h1>迷路</h1><strong>{puzzle.title} / {puzzle.width}×{puzzle.height}</strong></div><div className="stats"><span>手数: {state.history.length-1}</span><span>ヒント: {state.hintCount}回</span></div><div className="board-wrap"><div className="maze-board" aria-label="迷路盤面" role="grid" tabIndex={0} ref={board} onKeyDown={keyDown} style={{gridTemplateColumns:`repeat(${puzzle.width},1fr)`,['--maze-size' as string]:puzzle.width}}>{puzzle.passages.map((mask,index)=>{const position={row:Math.floor(index/puzzle.width),column:index%puzzle.width},isCurrent=samePosition(position,current),isStart=samePosition(position,puzzle.start),isGoal=samePosition(position,puzzle.goal),isHint=state.hintPosition&&samePosition(position,state.hintPosition);return <button key={index} role="gridcell" aria-label={`${position.row+1}行${position.column+1}列${isCurrent?' 現在位置':''}${isStart?' 入口':''}${isGoal?' ゴール':''}`} className={`maze-cell ${visits.has(keyOf(position))?'visited':''} ${isStart?'start':''} ${isGoal?'goal':''} ${isHint?'hinted':''}`} style={{borderTop:mask&UP?'0':'var(--wall)',borderRight:mask&RIGHT?'0':'var(--wall)',borderBottom:mask&DOWN?'0':'var(--wall)',borderLeft:mask&LEFT?'0':'var(--wall)'}} onClick={()=>apply(moveTo(puzzle,state,position))} disabled={complete}>{isStart&&<span className="endpoint">S</span>}{isGoal&&<span className="endpoint">G</span>}{isCurrent&&<span className="maze-player"/>}</button>})}</div></div><p className="operation-help">操作: 矢印キー / WASD / 隣接する通行可能マスをクリック</p><div className="actions"><button className="button hint" disabled={complete} onClick={()=>{const next=nextHint(puzzle,state);setState(next);if(next===state){setMessage('表示中のヒントを維持します。');return}const hint=next.hintPosition!,dr=hint.row-current.row,dc=hint.column-current.column;setMessage(`${directionNames[`${dr},${dc}`]}のマスへ進んでください。`)}}>次に進むマスを表示</button></div><MessagePanel message={message}/><GameFooter onBack={onBack} onLauncher={onLauncher} left={<><button className="button secondary" disabled={state.history.length<=1||complete} onClick={()=>{setState(undo(state));setMessage('一手戻しました。')}}>一手戻す</button><button className="button secondary" onClick={()=>{if(confirm('スタートに戻りますか？')){setState(initialGame(puzzle));setMessage('スタートに戻りました。')}}}>リセット</button></>}/></section></main>}
