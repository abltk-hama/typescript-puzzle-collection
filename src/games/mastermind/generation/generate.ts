import type { MastermindPuzzle } from '../domain/mastermind'
export const PROFILES={easy:{label:'かんたん',length:4,symbols:6,duplicates:false,attempts:10},medium:{label:'ふつう',length:4,symbols:6,duplicates:true,attempts:10},hard:{label:'むずかしい',length:5,symbols:8,duplicates:true,attempts:12}} as const
function mulberry32(seed:number){return()=>{let t=seed+=0x6D2B79F5;t=Math.imul(t^t>>>15,t|1);t^=t+Math.imul(t^t>>>7,t|61);return((t^t>>>14)>>>0)/4294967296}}
export function generateMastermind(seed:number,profile:keyof typeof PROFILES):MastermindPuzzle{const random=mulberry32(seed),data=PROFILES[profile],pool=Array.from({length:data.symbols},(_,index)=>index+1),secret:number[]=[];while(secret.length<data.length){const value=pool[Math.floor(random()*pool.length)];if(data.duplicates||!secret.includes(value))secret.push(value)}return{id:`mastermind-generated-${seed.toString(16)}`,title:'ランダム問題',difficulty:'generated',codeLength:data.length,symbolCount:data.symbols,allowDuplicates:data.duplicates,maxAttempts:data.attempts,secret,metadata:{source_type:'procedural',seed,profile,difficulty_is_estimate:true}}}
