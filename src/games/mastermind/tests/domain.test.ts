import { describe,expect,it } from 'vitest'
import { candidateCount,initialGame,isWon,revealPosition,scoreGuess,setSymbol,submitGuess,type MastermindPuzzle } from '../domain/mastermind'
import { generateMastermind } from '../generation/generate'
const puzzle:MastermindPuzzle={id:'test',title:'Test',difficulty:'medium',codeLength:4,symbolCount:6,allowDuplicates:true,maxAttempts:10,secret:[1,1,2,3],metadata:{}}
describe('Mastermind domain',()=>{it('scores duplicates without double counting',()=>expect(scoreGuess([1,1,2,3],[1,2,1,1])).toEqual([1,2]));it('submits a winning guess',()=>{let state=initialGame(puzzle);[1,1,2,3].forEach(value=>state=setSymbol(puzzle,state,value));state=submitGuess(puzzle,state);expect(isWon(puzzle,state)).toBe(true)});it('counts a candidate hint once for the same state',()=>{const first=candidateCount(puzzle,initialGame(puzzle)),second=candidateCount(puzzle,first.state);expect(first.count).toBe(1296);expect(first.state.hintCount).toBe(1);expect(second.counted).toBe(false);expect(second.state.hintCount).toBe(1)});it('reveals and locks a correct position',()=>{const state=revealPosition(puzzle,initialGame(puzzle),0);expect(state.revealed[0]).toBe(1);expect(state.current[0]).toBe(1)});it('generates reproducibly and respects easy uniqueness',()=>{const generated=generateMastermind(42,'easy');expect(generateMastermind(42,'easy')).toEqual(generated);expect(new Set(generated.secret).size).toBe(4)})})
