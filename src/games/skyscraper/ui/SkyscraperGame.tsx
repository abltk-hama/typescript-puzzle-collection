import { useEffect,useMemo,useRef,useState } from "react";
import { GameFooter,MessagePanel } from "../../../common/components/GameChrome";
import { loadAssistSettings,saveAssistSettings } from "../../../common/storage/gameAssistSettings";
import { addCandidateNotes,candidates,cleanCandidateNotes,clearCell,conflicts,enterNumber,findLogicalHint,initialSkyscraper,isComplete,isFixed,lineIndices,lineStatus,revealHint,visibilityMarkers,type Direction,type SkyscraperPuzzle,type SkyscraperState } from "../domain/skyscraper";
import { deleteSkyscraperSession,loadSkyscraperSession,saveSkyscraperSession } from "../data/sessions";
type Phase="loading"|"ask"|"play";
type DirectionSettings=Record<Direction,boolean>;
interface AssistSettings{directions:DirectionSettings;provisionalMarkers:boolean;impossibleWarnings:boolean}
const defaults:AssistSettings={directions:{left:true,right:true,top:true,bottom:true},provisionalMarkers:true,impossibleWarnings:true};
const directionMeta:Record<Direction,{label:string;arrow:string;color:string}>={left:{label:"左から",arrow:"→",color:"blue"},right:{label:"右から",arrow:"←",color:"orange"},top:{label:"上から",arrow:"↓",color:"green"},bottom:{label:"下から",arrow:"↑",color:"purple"}};
export function SkyscraperGame({puzzle,onBack,onLauncher}:{puzzle:SkyscraperPuzzle;onBack:()=>void;onLauncher:()=>void}){const[state,setState]=useState<SkyscraperState>(()=>initialSkyscraper(puzzle)),[phase,setPhase]=useState<Phase>("loading"),[message,setMessage]=useState("ゲームを開始しました。"),[assist,setAssist]=useState(()=>loadAssistSettings("skyscraper",defaults)),[settingsOpen,setSettingsOpen]=useState(false),[focusedClue,setFocusedClue]=useState<{direction:Direction;unit:number}|null>(null),saved=useRef<SkyscraperState|undefined>(undefined),board=useRef<HTMLDivElement>(null),complete=isComplete(puzzle,state),conflicting=useMemo(()=>conflicts(state.values,puzzle.size),[state.values,puzzle.size]),removable=useMemo(()=>cleanCandidateNotes(puzzle,state).removed,[puzzle,state]),digitCounts=useMemo(()=>Array.from({length:puzzle.size},(_,i)=>state.values.filter(value=>value===i+1).length),[state.values,puzzle.size]),markers=useMemo(()=>{const result:Record<Direction,Map<number,"confirmed"|"provisional">>={left:new Map(),right:new Map(),top:new Map(),bottom:new Map()};for(const direction of Object.keys(result)as Direction[])for(let unit=0;unit<puzzle.size;unit++)visibilityMarkers(state.values,puzzle.size,direction,unit).forEach((value,index)=>result[direction].set(index,value));return result},[state.values,puzzle.size]);
useEffect(()=>{loadSkyscraperSession(puzzle).then(found=>{saved.current=found;setPhase(found?"ask":"play")}).catch(()=>{setMessage("保存データを読み込めないため、最初から開始します。");setPhase("play")})},[puzzle]);
useEffect(()=>{if(phase!=="play")return;if(complete)deleteSkyscraperSession(puzzle.id);else if(state.values.some((value,index)=>value!==puzzle.givens[index])||state.notes.some(note=>note.length)||state.hintCount)saveSkyscraperSession(puzzle,state)},[state,phase,complete,puzzle]);
useEffect(()=>saveAssistSettings("skyscraper",assist),[assist]);useEffect(()=>{if(phase==="play")board.current?.focus()},[phase]);
const selectedValue=state.selected===null?0:state.values[state.selected],used=state.selected===null?new Set<number>():new Set([...lineIndices(puzzle.size,"left",Math.floor(state.selected/puzzle.size)),...lineIndices(puzzle.size,"top",state.selected%puzzle.size)].map(index=>state.values[index]).filter(Boolean));
function applyNumber(value:number){const next=enterNumber(puzzle,state,value);setState(next);if(next!==state){setFocusedClue(null);setMessage(state.inputMode==="note"?`候補 ${value} を切り替えました。`:`${value} を入力しました。`)}}
function clear(){const next=clearCell(puzzle,state);setState(next);if(next!==state)setMessage("選択マスを消去しました。")}
function keyDown(event:React.KeyboardEvent){if(new RegExp(`^[1-${puzzle.size}]$`).test(event.key)){event.preventDefault();applyNumber(Number(event.key))}else if(["Delete","Backspace","0"].includes(event.key)){event.preventDefault();clear()}else if(event.key==="n")setState({...state,inputMode:state.inputMode==="note"?"value":"note"})}
function clue(direction:Direction,unit:number){const value=puzzle.clues[direction][unit];if(!value)return <span className="skyscraper-clue empty" aria-hidden="true"/>;const status=lineStatus(puzzle,state.values,direction,unit),focused=focusedClue?.direction===direction&&focusedClue.unit===unit;return <button aria-label={`${directionMeta[direction].label} ${unit+1}番 ${value}`} className={`skyscraper-clue ${assist.impossibleWarnings?status:""} ${focused?"focused":""}`} onClick={()=>{setFocusedClue(focused?null:{direction,unit});setMessage(`${directionMeta[direction].label}${unit+1}列目：見える建物は ${value} 棟です。`)}}>{value}</button>}
if(phase==="loading")return <main className="shell"><section className="panel"><p>保存状態を確認しています…</p></section></main>;
if(phase==="ask")return <main className="shell"><section className="panel resume-dialog"><h1>保存済みの進捗</h1><p>この問題には保存済みの進捗があります。</p><div className="actions"><button className="button" onClick={()=>{setState(saved.current!);setMessage("保存済みの進捗から再開しました。");setPhase("play")}}>続きから</button><button className="button secondary" onClick={()=>{deleteSkyscraperSession(puzzle.id);setState(initialSkyscraper(puzzle));setPhase("play")}}>最初から</button><button className="button secondary" onClick={onBack}>キャンセル</button></div></section></main>;
return <main className="shell"><section className="panel"><div className="toolbar"><h1>スカイスクレーパー</h1><strong>{puzzle.title} / {puzzle.size}×{puzzle.size}</strong></div><div className="stats"><span>入力: {state.inputMode==="value"?"数字":"メモ"}</span><span>ヒント: {state.hintCount}回</span></div><div className="skyscraper-play"><div className="skyscraper-keypad" aria-label="数字キーパッド">{Array.from({length:puzzle.size},(_,i)=>i+1).map(value=><button key={value} aria-label={`数字 ${value}`} className={selectedValue===value?"active":""} disabled={complete||(state.inputMode==="value"&&used.has(value))} onClick={()=>applyNumber(value)}><span>{value}</span><small>{digitCounts[value-1]}/{puzzle.size}</small></button>)}<button className="wide" onClick={clear}>消去</button><button className={`wide ${state.inputMode==="note"?"active":""}`} onClick={()=>setState({...state,inputMode:state.inputMode==="note"?"value":"note"})}>メモ {state.inputMode==="note"?"ON":"OFF"}</button><div className="direction-toggles" aria-label="可視マーカー方向">{(Object.keys(directionMeta)as Direction[]).map(direction=><button key={direction} aria-pressed={assist.directions[direction]} className={`direction-${direction} ${assist.directions[direction]?"active":""}`} onClick={()=>setAssist({...assist,directions:{...assist.directions,[direction]:!assist.directions[direction]}})}>{directionMeta[direction].label} {directionMeta[direction].arrow}</button>)}</div></div><div ref={board} className={`skyscraper-board size-${puzzle.size}`} role="grid" aria-label="スカイスクレーパー盤面" tabIndex={0} onKeyDown={keyDown} style={{gridTemplateColumns:`repeat(${puzzle.size+2},var(--skyscraper-cell))`}}><span/>{Array.from({length:puzzle.size},(_,unit)=><span key={`top-${unit}`}>{clue("top",unit)}</span>)}<span/>{Array.from({length:puzzle.size},(_,row)=>[<span key={`left-${row}`}>{clue("left",row)}</span>,...Array.from({length:puzzle.size},(_,column)=>{const index=row*puzzle.size+column,value=state.values[index],focused=focusedClue?lineIndices(puzzle.size,focusedClue.direction,focusedClue.unit).includes(index):false;return <button key={index} role="gridcell" aria-label={`${row+1}行${column+1}列${value?` ${value}`:""}`} className={`${state.selected===index?"selected":""} ${isFixed(puzzle,state,index)?"fixed":""} ${state.hinted.includes(index)?"hinted":""} ${conflicting.has(index)?"conflict":""} ${focused?"clue-focused":""}`} onClick={()=>{setState({...state,selected:index});setFocusedClue(null)}} onContextMenu={event=>{event.preventDefault();setState({...state,selected:index,inputMode:"note"});setFocusedClue(null)}}>{value||<span className="skyscraper-notes">{Array.from({length:puzzle.size},(_,i)=><i key={i}>{state.notes[index].includes(i+1)?i+1:""}</i>)}</span>}{(Object.keys(directionMeta)as Direction[]).map(direction=>{const marker=markers[direction].get(index);return assist.directions[direction]&&marker&&(assist.provisionalMarkers||marker==="confirmed")?<i key={direction} aria-hidden="true" className={`visibility-dot ${direction} ${marker}`}/>:null})}</button>}),<span key={`right-${row}`}>{clue("right",row)}</span>])}<span/>{Array.from({length:puzzle.size},(_,unit)=><span key={`bottom-${unit}`}>{clue("bottom",unit)}</span>)}<span/></div></div><MessagePanel message={complete?`完成しました！（ヒント使用 ${state.hintCount}回）`:message}/><div className="assist-settings"><button className="button secondary" onClick={()=>setSettingsOpen(open=>!open)}>補助設定{settingsOpen?"を閉じる":"を開く"}</button>{settingsOpen&&<div className="assist-settings-panel"><label><input type="checkbox" checked={assist.provisionalMarkers} onChange={e=>setAssist({...assist,provisionalMarkers:e.target.checked})}/> 空欄より奥の暫定点を表示</label><label><input type="checkbox" checked={assist.impossibleWarnings} onChange={e=>setAssist({...assist,impossibleWarnings:e.target.checked})}/> 成立不能なヒントを警告</label><p>塗りつぶし点は確定、白抜き点は空欄を含む暫定表示です。</p></div>}</div><h2 className="action-heading">入力補助</h2><div className="actions"><button className="button secondary" disabled={state.selected===null||Boolean(selectedValue)} onClick={()=>{const next=addCandidateNotes(puzzle,state);setState(next);if(next!==state)setMessage(`候補 ${next.notes[next.selected!].join("・")||"なし"} をメモしました。`)}}>候補をメモ</button><button className="button secondary" disabled={!removable} onClick={()=>{const result=cleanCandidateNotes(puzzle,state);setState(result.state);setMessage(`不要なメモを${result.removed}件削除しました。`)}}>メモを整理{removable?`（${removable}）`:""}</button></div><h2 className="action-heading">ヒント</h2><div className="actions"><button className="button hint" disabled={state.selected===null||Boolean(selectedValue)} onClick={()=>{if(state.selected===null)return;const found=candidates(puzzle,state.values,state.selected);setState({...state,hintCount:state.hintCount+1});setMessage(`選択マスの候補: ${found.join("・")||"なし"}`)}}>選択マスの候補</button><button className="button hint" onClick={()=>{const found=findLogicalHint(puzzle,state.values);setState({...state,selected:found?.index??state.selected,hintCount:state.hintCount+1});setMessage(found?`${Math.floor(found.index/puzzle.size)+1}行${found.index%puzzle.size+1}列は ${found.value} に確定できます。`:"基本的な確定候補は見つかりませんでした。")}}>確定候補を探す</button><button className="button hint" onClick={()=>{const next=revealHint(puzzle,state);setState(next);setMessage(next===state?"開示できるマスはありません。":`${Math.floor(next.selected!/puzzle.size)+1}行${next.selected!%puzzle.size+1}列を開示しました。`)}}>1マス開示</button></div><GameFooter onBack={onBack} onLauncher={onLauncher} left={<button className="button secondary" onClick={()=>{if(confirm("盤面を初期状態に戻しますか？")){setState(initialSkyscraper(puzzle));deleteSkyscraperSession(puzzle.id);setMessage("盤面を初期状態に戻しました。")}}}>リセット</button>}/></section></main>}
