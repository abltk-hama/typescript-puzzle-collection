import { useEffect,useRef,useState } from 'react'
import { GameFooter,MessagePanel } from '../../../common/components/GameChrome'
import type { FifteenPuzzle,FifteenState } from '../domain/fifteen'
import { initialGame,isComplete,move,moveEmpty,nextHint,undo } from '../domain/fifteen'
import { deleteFifteenSession,loadFifteenSession,saveFifteenSession } from '../data/sessions'
type ResumeChoice='loading'|'ask'|'play'
export function FifteenGame({puzzle,onBack,onLauncher}:{puzzle:FifteenPuzzle;onBack:()=>void;onLauncher:()=>void}){const[state,setState]=useState<FifteenState>(()=>initialGame(puzzle)),[message,setMessage]=useState('ゲームを開始しました。'),[phase,setPhase]=useState<ResumeChoice>('loading'),saved=useRef<FifteenState|undefined>(undefined),boardRef=useRef<HTMLDivElement>(null),complete=isComplete(state);useEffect(()=>{loadFifteenSession(puzzle).then(session=>{saved.current=session;setPhase(session?'ask':'play')}).catch(()=>{setMessage('保存データを読み込めないため、最初から開始します。');setPhase('play')})},[puzzle]);useEffect(()=>{if(phase!=='play')return;if(complete||(!state.history.length&&!state.hintCount))deleteFifteenSession(puzzle.id);else saveFifteenSession(puzzle,state)},[state,complete,puzzle,phase]);useEffect(()=>{if(phase==='play')boardRef.current?.focus()},[phase]);function choose(tile:number){if(complete)return;const next=move(state,tile);if(next===state)return;setState(next);setMessage(isComplete(next)?`完成しました！（${next.history.length}手、ヒント使用 ${next.hintCount}回）`:`駒 ${tile} を動かしました。`)}function direction(row:number,column:number){const next=moveEmpty(state,row,column);if(next!==state){setState(next);setMessage(isComplete(next)?`完成しました！（${next.history.length}手、ヒント使用 ${next.hintCount}回）`:'矢印キーで駒を動かしました。')}}function keyDown(event:React.KeyboardEvent){const mapping:Record<string,[number,number]>={ArrowUp:[-1,0],ArrowDown:[1,0],ArrowLeft:[0,-1],ArrowRight:[0,1]};if(mapping[event.key]){event.preventDefault();direction(...mapping[event.key])}}if(phase==='loading')return <main className="shell"><section className="panel"><p>保存状態を確認しています…</p></section></main>;if(phase==='ask')return <main className="shell"><section className="panel resume-dialog"><h1>保存済みの進捗</h1><p>この問題には保存済みの進捗があります。</p><div className="actions"><button className="button" onClick={()=>{setState(saved.current!);setMessage('保存済みの進捗から再開しました。');setPhase('play')}}>続きから</button><button className="button secondary" onClick={()=>{deleteFifteenSession(puzzle.id);setState(initialGame(puzzle));setPhase('play')}}>最初から</button><button className="button secondary" onClick={onBack}>キャンセル</button></div></section></main>;return <main className="shell"><section className="panel"><div className="toolbar"><h1>15パズル</h1><strong>{puzzle.title}</strong></div><div className="stats"><span>手数: {state.history.length}</span><span>ヒント: {state.hintCount}回</span></div><div className="board-wrap"><div className="fifteen-board" role="grid" aria-label="15パズル盤面" tabIndex={0} ref={boardRef} onKeyDown={keyDown}>{state.tiles.map((tile,index)=><button key={tile} className={`fifteen-tile ${tile===0?'empty':''} ${tile===state.hintTile?'hinted':''}`} aria-label={tile===0?`空きマス ${Math.floor(index/4)+1}行${index%4+1}列`:`駒 ${tile}`} disabled={tile===0||complete} onClick={()=>choose(tile)}>{tile||''}</button>)}</div></div><p className="operation-help">マウス: 空きマスに隣接する駒を選択　／　キーボード: 矢印キーで空きマスを移動</p><div className="actions"><button className="button hint" disabled={complete} onClick={()=>{const next=nextHint(puzzle,state);setState(next);setMessage(next===state?'表示中のヒントを維持します。':`駒 ${next.hintTile} を動かしてください。`)}}>次に動かす駒を表示</button></div><MessagePanel message={message}/><GameFooter onBack={onBack} onLauncher={onLauncher} left={<><button className="button secondary" disabled={!state.history.length||complete} onClick={()=>{setState(undo(state));setMessage('一手戻しました。')}}>一手戻す</button><button className="button secondary" onClick={()=>{if(confirm('盤面を初期状態に戻しますか？')){setState(initialGame(puzzle));setMessage('盤面を初期状態に戻しました。')}}}>リセット</button></>}/></section></main>}
