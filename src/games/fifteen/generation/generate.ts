import type { FifteenPuzzle } from '../domain/fifteen'
import { GOAL,moveTile } from '../domain/fifteen'
export const PROFILES={easy:{label:'かんたん傾向',moves:15},medium:{label:'ふつう傾向',moves:40},hard:{label:'むずかしい傾向',moves:80}} as const
function mulberry32(seed:number){return()=>{let t=seed+=0x6D2B79F5;t=Math.imul(t^t>>>15,t|1);t^=t+Math.imul(t^t>>>7,t|61);return((t^t>>>14)>>>0)/4294967296}}
export function generateFifteen(seed:number,profile:keyof typeof PROFILES):FifteenPuzzle{const random=mulberry32(seed),count=PROFILES[profile].moves;let tiles=[...GOAL],previous:number|null=null;const scramble:number[]=[];for(let index=0;index<count;index++){let candidates=tiles.filter(tile=>tile!==0&&tile!==previous&&moveTile(tiles,tile)!==null);if(!candidates.length)candidates=tiles.filter(tile=>tile!==0&&moveTile(tiles,tile)!==null);const tile=candidates[Math.floor(random()*candidates.length)];tiles=moveTile(tiles,tile)!;scramble.push(tile);previous=tile}return{id:`fifteen-generated-${seed.toString(16)}`,title:'生成問題',difficulty:'generated',initialTiles:tiles,knownSolution:[...scramble].reverse(),metadata:{source_type:'procedural',seed,profile,shuffle_count:count,difficulty_is_estimate:true}}}
